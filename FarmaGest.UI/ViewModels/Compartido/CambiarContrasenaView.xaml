<Page
    x:Class="FarmaGest.UI.Views.Compartido.CambiarContrasenaView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="CambiarContrasenaView"
    d:DesignHeight="700"
    d:DesignWidth="900"
    Background="#F3F4F6"
    mc:Ignorable="d">

	<Grid>
		<Border
            Width="420"
            Padding="40,44"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="White"
            CornerRadius="16">
			<Border.Effect>
				<DropShadowEffect
                    BlurRadius="24"
                    Direction="270"
                    Opacity="0.12"
                    ShadowDepth="4"
                    Color="Black" />
			</Border.Effect>

			<StackPanel>
				<TextBlock
                    HorizontalAlignment="Center"
                    FontFamily="Georgia"
                    FontSize="24"
                    FontWeight="Bold"
                    Text="Cambiar contraseña" />

				<TextBlock
                    Margin="0,6,0,20"
                    HorizontalAlignment="Center"
                    FontSize="13"
                    Foreground="#6B7280"
                    TextWrapping="Wrap"
                    Text="Por seguridad, debe definir una nueva contraseña antes de continuar." />

				<TextBlock
                    Margin="0,0,0,12"
                    HorizontalAlignment="Center"
                    FontSize="13"
                    FontWeight="SemiBold"
                    Foreground="#DC2626"
                    Text="{Binding MensajeError}"
                    TextWrapping="Wrap" />

				<TextBlock Margin="0,0,0,6" FontWeight="SemiBold" Text="Nueva contraseña" />
				<ui:PasswordBox
                    x:Name="TxtNuevaContrasena"
                    Margin="0,0,0,18"
                    Padding="12,10"
                    FontSize="14"
                    PasswordChanged="TxtNuevaContrasena_PasswordChanged"
                    PlaceholderText="••••••••" />

				<TextBlock Margin="0,0,0,6" FontWeight="SemiBold" Text="Confirmar contraseña" />
				<ui:PasswordBox
                    x:Name="TxtConfirmarContrasena"
                    Margin="0,0,0,24"
                    Padding="12,10"
                    FontSize="14"
                    PasswordChanged="TxtConfirmarContrasena_PasswordChanged"
                    PlaceholderText="••••••••" />

				<ui:Button
                    Height="44"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Center"
                    Appearance="Primary"
                    Background="#16A34A"
                    Command="{Binding ConfirmarCambioCommand}"
                    Content="Guardar y continuar"
                    FontSize="14"
                    FontWeight="SemiBold" />
			</StackPanel>
		</Border>
	</Grid>
</Page>